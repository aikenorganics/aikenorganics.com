const {req} = locals

json.set({
  busy: false,
  canEdit: req.canEdit,
  cart: req.cart.cart
})

json.set('currentUser', () => {
  const {user} = req
  <%! users/user.ejson %>
})

json.set('market', () => {
  const {market} = req
  <%! markets/market.ejson %>
})

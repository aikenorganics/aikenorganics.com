<!doctype html>
<html>
<head>
  <% include ../head %>
</head>
<body>

<% include ../nav %>

<div class='container'>
  <h1>Users</h1>
  <table class='table'>
    <thead>
      <tr>
        <th></th>
        <th>Email</th>
        <th>First</th>
        <th>Last</th>
        <th>Phone</th>
      </tr>
    </thead>
    <tbody>
      <% users.forEach(function(user) { %>
        <tr>
          <td><a href='/users/<%= user.id %>/edit'>Edit</a></td>
          <td><%= user.email %></td>
          <td><%= user.first || '-' %></td>
          <td><%= user.last || '-' %></td>
          <td><%= user.phone || '-' %></td>
        </tr>
      <% }); %>
    </tbody>
  </table>
</div>

<% include ../footer %>

<script src='/js/app.js'></script>

</body>
</html>

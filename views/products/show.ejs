<!doctype html>
<html>
<head>
  <% include ../head %>
</head>
<body>

<% include ../nav %>

<main class='container'>

  <% include ../subnav %>

  <h1><%= product.name %></h1>
  <% if (locals.admin) { %>
    <span class='pull-right'>
      <a href='/products/<%= product.id %>/edit'>Edit</a>
    </span>
  <% } %>
  $<%= product.cost %>
  per
  <%= product.unit %>
  &bull;
  <%= product.supply %> Available
  &bull;
  <a href='/growers/<%= grower.id %>'>
    <%= grower.name %>
  </a>
  &bull;
  <%= category.name %>
  <div class='clearfix'>
    <hr>
  </div>
  <div class='row'>
    <div class='col-md-8'>
      <%- marked(product.description) %>
    </div>
    <div class='col-md-4'>
      <img class='img-rounded' src='<%= imageUrl(product, 'medium') %>'>
    </div>
  </div>

</main>

<% include ../footer %>

<script src='/js/app.js'></script>

</body>
</html>
